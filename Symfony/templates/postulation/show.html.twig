{% extends 'base.html.twig' %}

{% block title %}Postulation{% endblock %}

{% block body %}
    <h1>Postulation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPostulation</th>
                <td>{{ postulation.idPostulation }}</td>
            </tr>
            <tr>
                <th>Isaccepted</th>
                <td>{{ postulation.isaccepted }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_postulation_index') }}">back to list</a>

    <a href="{{ path('app_postulation_edit', {'idPostulation': postulation.idPostulation}) }}">edit</a>

    {{ include('postulation/_delete_form.html.twig') }}
{% endblock %}
